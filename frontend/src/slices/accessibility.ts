import type { StateCreator } from "zustand";
import type { AccessibilitySlice } from "../types/accessibility";

export const createAccessibilitySlice: StateCreator<
  AccessibilitySlice
> = () => ({});
